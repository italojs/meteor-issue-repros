{"code":"module.export({buildUnignorePatterns:()=>buildUnignorePatterns});/**\n * Build gitignore-style \"unignore\" patterns for specific files/folders.\n *\n * Rules:\n *  - Files:  !a/  !a/b/  !a/b/c.txt\n *  - Folders (must end with '/'):\n *            !a/  !a/b/  !a/b/c/  !a/b/c/**\n *\n * @param {string[]} inputPaths  Paths to keep. Use '/' for dirs (e.g. 'assets/public/').\n * @param {Object} [options]\n * @param {boolean} [options.includeAllAncestors=true]  If false, only include the immediate parent dir.\n * @param {boolean} [options.includeGlobForDirs=true]   Emit '**' for directories.\n * @param {number} [options.skipLevel=0]               Skip this many levels from the beginning.\n * @returns {string[]} Negation patterns, in correct order.\n */ function buildUnignorePatterns(inputPaths, { includeAllAncestors = true, includeGlobForDirs = true, skipLevel = 0 } = {}) {\n    const out = [];\n    const seen = new Set();\n    const push = (p)=>{\n        if (!seen.has(p)) {\n            seen.add(p);\n            out.push(p);\n        }\n    };\n    for (let raw of inputPaths){\n        if (!raw || typeof raw !== 'string') continue;\n        // Normalize: forward slashes, drop leading './', collapse double slashes\n        let anchored = raw.startsWith('/');\n        let p = raw.replace(/\\\\/g, '/').replace(/^\\.\\/+/, '').replace(/\\/{2,}/g, '/');\n        // detect dir by trailing slash\n        const isDir = p.endsWith('/');\n        // strip leading + trailing slashes for splitting, but remember anchoring\n        const core = p.replace(/^\\/+/, '').replace(/\\/+$/, '');\n        if (!core) continue;\n        const parts = core.split('/');\n        // Process based on skipLevel\n        if (skipLevel >= parts.length) {\n            continue;\n        }\n        // Ancestors (top-down)\n        if (includeAllAncestors) {\n            // Start from skipLevel + 1 to skip the specified number of levels\n            const startLevel = Math.max(1, skipLevel + 1);\n            for(let i = startLevel; i <= parts.length - 1; i++){\n                const anc = (anchored ? '/' : '') + parts.slice(0, i).join('/') + '/';\n                push('!' + anc);\n            }\n        } else if (parts.length > 1) {\n            // Only immediate parent\n            // For minimal mode with skipLevel, we need to check if the parent is at a level we should skip\n            if (skipLevel < parts.length - 1) {\n                // Check if the parent's level is greater than skipLevel\n                const parentLevel = parts.length - 1;\n                if (parentLevel > skipLevel) {\n                    const parent = (anchored ? '/' : '') + parts.slice(0, parts.length - 1).join('/') + '/';\n                    push('!' + parent);\n                }\n            }\n        }\n        // Add the file/directory pattern\n        if (isDir) {\n            const dir = (anchored ? '/' : '') + parts.join('/') + '/';\n            push('!' + dir);\n            if (includeGlobForDirs) push('!' + dir + '**');\n        } else {\n            const file = (anchored ? '/' : '') + parts.join('/');\n            push('!' + file);\n        }\n    }\n    return out;\n}\n","map":{"version":3,"sources":["packages/tools-core/lib/ignore.js"],"sourcesContent":["/**\n * Build gitignore-style \"unignore\" patterns for specific files/folders.\n *\n * Rules:\n *  - Files:  !a/  !a/b/  !a/b/c.txt\n *  - Folders (must end with '/'):\n *            !a/  !a/b/  !a/b/c/  !a/b/c/**\n *\n * @param {string[]} inputPaths  Paths to keep. Use '/' for dirs (e.g. 'assets/public/').\n * @param {Object} [options]\n * @param {boolean} [options.includeAllAncestors=true]  If false, only include the immediate parent dir.\n * @param {boolean} [options.includeGlobForDirs=true]   Emit '**' for directories.\n * @param {number} [options.skipLevel=0]               Skip this many levels from the beginning.\n * @returns {string[]} Negation patterns, in correct order.\n */\nexport function buildUnignorePatterns(inputPaths, {\n  includeAllAncestors = true,\n  includeGlobForDirs = true,\n  skipLevel = 0,\n} = {}) {\n  const out = [];\n  const seen = new Set();\n\n  const push = (p) => {\n    if (!seen.has(p)) {\n      seen.add(p);\n      out.push(p);\n    }\n  };\n\n  for (let raw of inputPaths) {\n    if (!raw || typeof raw !== 'string') continue;\n\n    // Normalize: forward slashes, drop leading './', collapse double slashes\n    let anchored = raw.startsWith('/');\n    let p = raw.replace(/\\\\/g, '/')\n      .replace(/^\\.\\/+/, '')\n      .replace(/\\/{2,}/g, '/');\n\n    // detect dir by trailing slash\n    const isDir = p.endsWith('/');\n    // strip leading + trailing slashes for splitting, but remember anchoring\n    const core = p.replace(/^\\/+/, '').replace(/\\/+$/, '');\n    if (!core) continue;\n\n    const parts = core.split('/');\n\n    // Process based on skipLevel\n    if (skipLevel >= parts.length) {\n      // Skip everything if skipLevel is greater than or equal to the number of parts\n      continue;\n    }\n\n    // Ancestors (top-down)\n    if (includeAllAncestors) {\n      // Start from skipLevel + 1 to skip the specified number of levels\n      const startLevel = Math.max(1, skipLevel + 1);\n      for (let i = startLevel; i <= parts.length - 1; i++) {\n        const anc = (anchored ? '/' : '') + parts.slice(0, i).join('/') + '/';\n        push('!' + anc);\n      }\n    } else if (parts.length > 1) {\n      // Only immediate parent\n      // For minimal mode with skipLevel, we need to check if the parent is at a level we should skip\n      if (skipLevel < parts.length - 1) {\n        // Check if the parent's level is greater than skipLevel\n        const parentLevel = parts.length - 1;\n        if (parentLevel > skipLevel) {\n          const parent = (anchored ? '/' : '') + parts.slice(0, parts.length - 1).join('/') + '/';\n          push('!' + parent);\n        }\n      }\n    }\n\n    // Add the file/directory pattern\n    if (isDir) {\n      const dir = (anchored ? '/' : '') + parts.join('/') + '/';\n      push('!' + dir);\n      if (includeGlobForDirs) push('!' + dir + '**');\n    } else {\n      const file = (anchored ? '/' : '') + parts.join('/');\n      push('!' + file);\n    }\n  }\n\n  return out;\n}\n"],"names":["buildUnignorePatterns","inputPaths","includeAllAncestors","includeGlobForDirs","skipLevel","out","seen","Set","push","p","has","add","raw","anchored","startsWith","replace","isDir","endsWith","core","parts","split","length","startLevel","Math","max","i","anc","slice","join","parentLevel","parent","dir","file"],"mappings":"AAAA;;;;;;;;;;;;;;CAcC,GACD,OAAO,SAASA,sBAAsBC,UAAU,EAAE,EAChDC,sBAAsB,IAAI,EAC1BC,qBAAqB,IAAI,EACzBC,YAAY,CAAC,EACd,GAAG,CAAC,CAAC;IACJ,MAAMC,MAAM,EAAE;IACd,MAAMC,OAAO,IAAIC;IAEjB,MAAMC,OAAO,CAACC;QACZ,IAAI,CAACH,KAAKI,GAAG,CAACD,IAAI;YAChBH,KAAKK,GAAG,CAACF;YACTJ,IAAIG,IAAI,CAACC;QACX;IACF;IAEA,KAAK,IAAIG,OAAOX,WAAY;QAC1B,IAAI,CAACW,OAAO,OAAOA,QAAQ,UAAU;QAErC,yEAAyE;QACzE,IAAIC,WAAWD,IAAIE,UAAU,CAAC;QAC9B,IAAIL,IAAIG,IAAIG,OAAO,CAAC,OAAO,KACxBA,OAAO,CAAC,UAAU,IAClBA,OAAO,CAAC,WAAW;QAEtB,+BAA+B;QAC/B,MAAMC,QAAQP,EAAEQ,QAAQ,CAAC;QACzB,yEAAyE;QACzE,MAAMC,OAAOT,EAAEM,OAAO,CAAC,QAAQ,IAAIA,OAAO,CAAC,QAAQ;QACnD,IAAI,CAACG,MAAM;QAEX,MAAMC,QAAQD,KAAKE,KAAK,CAAC;QAEzB,6BAA6B;QAC7B,IAAIhB,aAAae,MAAME,MAAM,EAAE;YAE7B;QACF;QAEA,uBAAuB;QACvB,IAAInB,qBAAqB;YACvB,kEAAkE;YAClE,MAAMoB,aAAaC,KAAKC,GAAG,CAAC,GAAGpB,YAAY;YAC3C,IAAK,IAAIqB,IAAIH,YAAYG,KAAKN,MAAME,MAAM,GAAG,GAAGI,IAAK;gBACnD,MAAMC,MAAM,AAACb,CAAAA,WAAW,MAAM,EAAC,IAAKM,MAAMQ,KAAK,CAAC,GAAGF,GAAGG,IAAI,CAAC,OAAO;gBAClEpB,KAAK,MAAMkB;YACb;QACF,OAAO,IAAIP,MAAME,MAAM,GAAG,GAAG;YAC3B,wBAAwB;YACxB,+FAA+F;YAC/F,IAAIjB,YAAYe,MAAME,MAAM,GAAG,GAAG;gBAChC,wDAAwD;gBACxD,MAAMQ,cAAcV,MAAME,MAAM,GAAG;gBACnC,IAAIQ,cAAczB,WAAW;oBAC3B,MAAM0B,SAAS,AAACjB,CAAAA,WAAW,MAAM,EAAC,IAAKM,MAAMQ,KAAK,CAAC,GAAGR,MAAME,MAAM,GAAG,GAAGO,IAAI,CAAC,OAAO;oBACpFpB,KAAK,MAAMsB;gBACb;YACF;QACF;QAEA,iCAAiC;QACjC,IAAId,OAAO;YACT,MAAMe,MAAM,AAAClB,CAAAA,WAAW,MAAM,EAAC,IAAKM,MAAMS,IAAI,CAAC,OAAO;YACtDpB,KAAK,MAAMuB;YACX,IAAI5B,oBAAoBK,KAAK,MAAMuB,MAAM;QAC3C,OAAO;YACL,MAAMC,OAAO,AAACnB,CAAAA,WAAW,MAAM,EAAC,IAAKM,MAAMS,IAAI,CAAC;YAChDpB,KAAK,MAAMwB;QACb;IACF;IAEA,OAAO3B;AACT","file":"packages/tools-core/lib/ignore.js.map"},"sourceType":"module"}